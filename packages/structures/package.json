{
  "name": "@monitor/structures",
  "version": "0.1.0",
  "description": "common data structures/definitions used in shopify monitor",
  "main": "dist/index.js",
  "license": "MIT",
  "private": true,
  "files": [
    "dist/*"
  ],
  "scripts": {
    "build": "babel --root-mode upward --no-comments -d dist src",
    "test": "jest",
    "postinstall": "yarn run build",
    "lint": "eslint --ext js src/",
    "lint:fix": "eslint --fix --ext js src/"
  },
  "dependencies": {
    "prop-types": "^15.7.2",
    "whatwg-url": "^7.0.0"
  }
}
